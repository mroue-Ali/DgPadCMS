@model PostType

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>PostType</h4>
    <hr />
    <dl class="row">
       
      
        <dt class = "col-sm-2">
            Title
        </dt>
        <dd class = "col-sm-10">
            @Model.Title
        </dd>
        
        <dt class = "col-sm-2">
            MediaChecked

        </dt>
        <dd class = "col-sm-10">
            @Model.MediaChecked
        </dd>
        
            <dt class="col-sm-2">taxonomies</dt>

        <dd class="col-sm-10">

            @foreach (var item in ViewBag.selectedTaxonomies)
                    {

                   
                        <label class="control-label">@item.Taxonomy.Name</label>

                        <br />
                }
                
                         </dd>
             
        
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
